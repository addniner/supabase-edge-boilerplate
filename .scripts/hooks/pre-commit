#!/bin/bash
# Pre-commit hook: Terraform 자동 포맷팅

# staging된 .tf 파일이 있는지 확인
TF_FILES=$(git diff --cached --name-only --diff-filter=ACM | grep '\.tf$')

if [ -n "$TF_FILES" ]; then
  terraform fmt -recursive infra/
  git add $TF_FILES
fi
